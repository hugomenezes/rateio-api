const Controller = require('../../../controller/coastcenter/index');
describe("Coast center controller should:", function () {
    var a;

    it("Creates a new coast center.", function () {
        expect(false).toBe(true);
    });
});
